dependencies:
  pre:
    - echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc

dependencies:
  override:
    - npm install

test:
  override:
    - npm run -s lint
    - npm test

deployment:
  npm:
    tag: /v[0-9]+(\.[0-9]+)*/
        commands:
      - npm publish